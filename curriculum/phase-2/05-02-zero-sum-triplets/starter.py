def zero_sum_triplets(values: list[int]) -> list[list[int]]:
    """Return every distinct non-decreasing triplet from three different positions summing to 0, in lexicographic order."""
    raise NotImplementedError
