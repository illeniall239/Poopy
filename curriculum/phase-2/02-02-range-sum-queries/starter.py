def range_sums(values: list[int], queries: list[tuple[int, int]]) -> list[int]:
    """Return values[i] + ... + values[j] (inclusive) for each query (i, j), in query order."""
    raise NotImplementedError
