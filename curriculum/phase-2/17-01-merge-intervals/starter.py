def merge_intervals(intervals: list[list[int]]) -> list[list[int]]:
    """Return new [start, end] pairs covering the same points with overlaps (including touching ends) merged, sorted by start."""
    raise NotImplementedError
