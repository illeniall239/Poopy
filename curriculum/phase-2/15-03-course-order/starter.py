def course_order(num_courses: int, prerequisites: list[list[int]]) -> list[int]:
    """Any order of all courses respecting every [course, required] pair, or [] if impossible."""
    raise NotImplementedError
