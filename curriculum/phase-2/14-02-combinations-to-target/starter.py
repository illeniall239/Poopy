def combinations_to_target(candidates: list[int], target: int) -> list[list[int]]:
    """Every combination (candidates reusable) summing to target, no duplicates, in any order."""
    raise NotImplementedError
