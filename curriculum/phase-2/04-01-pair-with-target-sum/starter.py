def pair_with_target_sum(values: list[int], target: int) -> list[int]:
    """Return [i, j] with i < j and values[i] + values[j] == target (smallest j, then smallest i), or [-1, -1]."""
    raise NotImplementedError
