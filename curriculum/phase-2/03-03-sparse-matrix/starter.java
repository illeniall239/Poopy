public class Solution {
    /** A rows x cols matrix that stores only its non-zero cells. */
    public static class SparseMatrix {
        /** Creates a rows x cols matrix of zeros. */
        public SparseMatrix(int rows, int cols) {
            throw new UnsupportedOperationException("Not implemented");
        }

        /** Sets a cell; setting 0 removes it from storage. */
        public void set(int row, int col, int value) {
            throw new UnsupportedOperationException("Not implemented");
        }

        /** Returns the cell's value, 0 if not stored. */
        public int get(int row, int col) {
            throw new UnsupportedOperationException("Not implemented");
        }

        /** Returns how many cells are stored. */
        public int nonZeroCount() {
            throw new UnsupportedOperationException("Not implemented");
        }

        /** Returns the matrix times vector, in O(rows + non-zero cells). */
        public long[] multiplyVector(int[] vector) {
            throw new UnsupportedOperationException("Not implemented");
        }
    }
}
