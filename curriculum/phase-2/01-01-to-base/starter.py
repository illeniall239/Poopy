def to_base(n: int, base: int) -> str:
    """Return n written in base (2-16) using digits 0-9 and a-f; 0 is "0"."""
    raise NotImplementedError


def from_base(text: str, base: int) -> int:
    """Return the value of text, a number written in base (2-16) with lowercase letters."""
    raise NotImplementedError
