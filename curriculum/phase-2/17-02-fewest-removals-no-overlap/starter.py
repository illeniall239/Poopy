def fewest_removals(intervals: list[list[int]]) -> int:
    """Smallest number of intervals to remove so that no two remaining ones overlap. Must not change the input."""
    raise NotImplementedError
