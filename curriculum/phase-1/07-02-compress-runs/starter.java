public class Solution {
    /** Replaces each run of the same character by the character followed by the run length (length 1 writes only the character). Case-sensitive. */
    public static String compressRuns(String text) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
