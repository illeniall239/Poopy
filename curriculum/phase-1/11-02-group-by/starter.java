import java.util.List;
import java.util.Map;
import java.util.function.Function;

public class Solution {
    /** Calls keyOf once per item and maps each returned key to the items that produced it, in input order; empty list gives an empty map. Does not change items. */
    public static <T> Map<String, List<T>> groupBy(List<T> items, Function<T, String> keyOf) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
