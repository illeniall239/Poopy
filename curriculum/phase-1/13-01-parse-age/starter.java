public class Solution {
    /**
     * Parses an age typed as text. Ignores spaces at the start and end; leading zeros are allowed.
     * Empty → IllegalArgumentException("Age is empty");
     * any non-digit character → NumberFormatException("Age must be a whole number, got \"<input>\"") with the original input;
     * greater than 150 → IllegalArgumentException("Age must be between 0 and 150, got <number>").
     */
    public static int parseAge(String input) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
