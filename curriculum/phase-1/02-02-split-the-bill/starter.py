def split_bill(total_dollars: float, people: int) -> dict[str, int]:
    """Return {"shareCents": ..., "peopleWithExtraCent": ...} so that everyone together pays exactly the total."""
    raise NotImplementedError
