def celsius_to_fahrenheit(celsius: float) -> float:
    """Return celsius * 9 / 5 + 32, rounded to one decimal place (halves round up)."""
    raise NotImplementedError


def fahrenheit_to_celsius(fahrenheit: float) -> float:
    """Return (fahrenheit - 32) * 5 / 9, rounded to one decimal place (halves round up)."""
    raise NotImplementedError
