def caesar_shift(text: str, shift: int) -> str:
    """Return text with every letter moved shift places along the alphabet (wrapping, keeping case); other characters unchanged."""
    raise NotImplementedError
