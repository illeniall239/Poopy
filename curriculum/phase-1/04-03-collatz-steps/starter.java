public class Solution {
    /** steps to reach 1, and the largest number seen in the sequence (including n itself). */
    public record CollatzResult(int steps, long peak) {}

    /** Runs the Collatz sequence from n until it reaches 1. The peak can exceed int range, hence long. */
    public static CollatzResult collatzSteps(int n) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
