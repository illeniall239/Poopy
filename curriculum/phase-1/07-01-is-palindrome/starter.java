public class Solution {
    /** True if text reads the same backwards, counting only English letters and ignoring case. No letters at all counts as a palindrome. */
    public static boolean isPalindrome(String text) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
