import java.util.List;
import java.util.Optional;

public class Solution {
    /** The final element, keeping the caller's element type, or empty for an empty list. */
    public static <T> Optional<T> last(List<T> items) {
        throw new UnsupportedOperationException("Not implemented");
    }

    /** Consecutive groups of size elements (the final group may be shorter); empty list gives an empty list. Throws IllegalArgumentException if size is below 1. Does not change items. */
    public static <T> List<List<T>> chunk(List<T> items, int size) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
