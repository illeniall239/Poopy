public class Solution {
    /** Where a seat is, with row and column both starting at 1. */
    public record Seat(int row, int column) {}

    /** Returns the row and column of seat number seatNumber when every row has seatsPerRow seats. */
    public static Seat findSeat(int seatNumber, int seatsPerRow) {
        throw new UnsupportedOperationException("Not implemented");
    }
}
