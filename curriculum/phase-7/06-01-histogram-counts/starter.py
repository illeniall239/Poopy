def histogram(values: list[float], bins: int) -> tuple[list[int], list[float]]:
    """(counts, edges) for equal-width bins over [min, max] with the last bin closed; ValueError on empty input."""
    raise NotImplementedError
