import numpy as np


def pairwise_distances(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """Return the (n, m) matrix of Euclidean distances between rows of a (n, d) and rows of b (m, d), without loops."""
    raise NotImplementedError
