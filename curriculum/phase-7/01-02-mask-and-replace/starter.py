import numpy as np


def replace_outliers(x: np.ndarray, k: float) -> np.ndarray:
    """Return a copy of x where values more than k population stds from their column mean become that column's median."""
    raise NotImplementedError
