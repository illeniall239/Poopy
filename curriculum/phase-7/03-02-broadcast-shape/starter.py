def broadcast_shape(a: tuple[int, ...], b: tuple[int, ...]) -> tuple[int, ...]:
    """NumPy broadcast result shape of shapes a and b; ValueError when they are incompatible."""
    raise NotImplementedError
