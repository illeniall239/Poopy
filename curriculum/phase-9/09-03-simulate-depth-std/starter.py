import numpy as np


def simulate_depth_std(depth: int, width: int, init_std: float, act: str, seed: int) -> list[float]:
    """Push 1000 unit-normal inputs through depth random layers; return the activation std after each layer."""
    raise NotImplementedError
