import numpy as np


def linear_backward(X: np.ndarray, W: np.ndarray, dY: np.ndarray) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
    """Given dY for Y = X @ W + b, return (dX, dW, db) with the shapes of X, W and b."""
    raise NotImplementedError
