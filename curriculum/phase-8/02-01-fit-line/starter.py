def fit_line(xs: list[float], ys: list[float]) -> tuple[float, float]:
    """Least-squares (w, b) for y = w*x + b; ValueError on mismatch, < 2 points or constant xs."""
    raise NotImplementedError
