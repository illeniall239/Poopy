def gd_linreg_1d(xs: list[float], ys: list[float], lr: float, epochs: int) -> tuple[float, float, list[float]]:
    """Full-batch gradient descent on MSE for y = w*x + b from (0, 0); returns (w, b, loss_history)."""
    raise NotImplementedError
