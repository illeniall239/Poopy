import numpy as np


def explained_variance_ratio(X: np.ndarray, k: int) -> np.ndarray:
    """Share of the total variance along each of the top k principal components, from the SVD of centered X."""
    raise NotImplementedError
