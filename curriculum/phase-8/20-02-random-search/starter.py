import random


def random_search(space: dict, n: int, rng: random.Random) -> list[dict]:
    """n configurations sampled from lists, uniform and log-uniform ranges, names in sorted order."""
    raise NotImplementedError
